// creo mi contexto, donde voy a almacenar las variables y estados
// que quiero usar por afuera

import {createContext} from 'react';

const newContext = createContext();
// este NewContext es el que va a almacenar toda la info

export default newContext;
